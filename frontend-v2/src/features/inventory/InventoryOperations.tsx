import { FormEvent, useCallback, useEffect, useMemo, useState } from 'react'
import { CircleAlert, Edit3, FlaskConical, PackagePlus, Plus, RefreshCw, Trash2, Truck, X } from 'lucide-react'
import { apiJson, apiRequest, arrayFrom, formatDate, formatMoney, numberValue, textValue } from '../../lib/http'

export type InventorySection = 'oils' | 'suppliers' | 'purchase-orders' | 'manufacturing'
type Props = { token: string; onUnauthorized: () => void; section: InventorySection }
type Row = Record<string, unknown>

const emptyOil = { name: '', inventory_item_id: '', supplier_id: '', purchase_price_per_kg: '', grams_left: '' }
const emptySupplier = { name: '', contact_info: '' }
const emptyPO = { supplier_id: '', oil_inventory_id: '', quantity_grams: '', unit_price_per_kg: '', purchase_date: new Date().toISOString().slice(0, 10) }
const emptyManufacturing = { manufacturing_date: new Date().toISOString().slice(0, 10), notes: '', oil_inventory_id: '', quantity_grams: '', inventory_item_id: '', quantity_produced: '' }

function nestedName(value: unknown) { return value && typeof value === 'object' ? textValue((value as Row).name || (value as Row).title) : '—' }

export function InventoryOperations({ token, onUnauthorized, section }: Props) {
  const [rows, setRows] = useState<Row[]>([])
  const [oils, setOils] = useState<Row[]>([])
  const [suppliers, setSuppliers] = useState<Row[]>([])
  const [products, setProducts] = useState<Row[]>([])
  const [isLoading, setIsLoading] = useState(true)
  const [isWorking, setIsWorking] = useState(false)
  const [error, setError] = useState('')
  const [notice, setNotice] = useState('')
  const [modal, setModal] = useState(false)
  const [editingId, setEditingId] = useState<string | null>(null)
  const [oilForm, setOilForm] = useState(emptyOil)
  const [supplierForm, setSupplierForm] = useState(emptySupplier)
  const [poForm, setPOForm] = useState(emptyPO)
  const [manufacturingForm, setManufacturingForm] = useState(emptyManufacturing)

  const endpoint = section === 'oils' ? '/api/inventory/oil?limit=100' : section === 'suppliers' ? '/api/inventory/suppliers' : section === 'purchase-orders' ? '/api/inventory/po?limit=100' : '/api/inventory/manufacturing?limit=100'
  const load = useCallback(async () => {
    setIsLoading(true); setError('')
    try {
      const requests: Promise<unknown>[] = [apiJson<unknown>(token, onUnauthorized, endpoint)]
      if (section !== 'suppliers') requests.push(apiJson<unknown>(token, onUnauthorized, '/api/inventory/oil?limit=100'))
      if (section !== 'suppliers') requests.push(apiJson<unknown>(token, onUnauthorized, '/api/inventory/suppliers'))
      if (section === 'oils' || section === 'manufacturing') requests.push(apiJson<unknown>(token, onUnauthorized, '/api/inventory?limit=100'))
      const results = await Promise.all(requests)
      setRows(arrayFrom(results[0], section === 'oils' ? 'items' : section === 'purchase-orders' || section === 'manufacturing' ? 'items' : undefined))
      let resultIndex = 1
      if (section !== 'suppliers') { setOils(arrayFrom(results[resultIndex], 'items')); resultIndex += 1 }
      if (section !== 'suppliers') { setSuppliers(arrayFrom(results[resultIndex], 'suppliers')); resultIndex += 1 }
      if (section === 'oils' || section === 'manufacturing') setProducts(arrayFrom(results[resultIndex], 'items'))
    } catch (caughtError) { setError(caughtError instanceof Error ? caughtError.message : 'Unable to load inventory operations') } finally { setIsLoading(false) }
  }, [endpoint, onUnauthorized, section, token])
  useEffect(() => { void load() }, [load])

  const title = section === 'oils' ? 'Oil inventory' : section === 'suppliers' ? 'Suppliers' : section === 'purchase-orders' ? 'Purchase orders' : 'Manufacturing'
  const icon = section === 'oils' ? <FlaskConical size={18} aria-hidden="true" /> : section === 'suppliers' ? <Truck size={18} aria-hidden="true" /> : section === 'purchase-orders' ? <PackagePlus size={18} aria-hidden="true" /> : <PackagePlus size={18} aria-hidden="true" />
  const summary = useMemo(() => section === 'oils' ? { primary: rows.length, primaryLabel: 'Oil records', secondary: rows.reduce((sum, row) => sum + numberValue(row.grams_left), 0), secondaryLabel: 'Grams available' } : section === 'suppliers' ? { primary: rows.length, primaryLabel: 'Suppliers', secondary: rows.reduce((sum, row) => sum + numberValue(row.oils_count), 0), secondaryLabel: 'Oil relationships' } : section === 'purchase-orders' ? { primary: rows.length, primaryLabel: 'Purchase records', secondary: rows.reduce((sum, row) => sum + numberValue(row.total_price), 0), secondaryLabel: 'Recorded value' } : { primary: rows.length, primaryLabel: 'Production runs', secondary: rows.reduce((sum, row) => sum + (Array.isArray(row.products) ? row.products.reduce((count, product) => count + numberValue((product as Row).quantity_produced), 0) : 0), 0), secondaryLabel: 'Units produced' }, [rows, section])

  const openCreate = () => { setEditingId(null); setOilForm(emptyOil); setSupplierForm(emptySupplier); setPOForm(emptyPO); setManufacturingForm(emptyManufacturing); setModal(true) }
  const openEdit = (row: Row) => { setEditingId(textValue(row.id, '')); if (section === 'oils') setOilForm({ name: textValue(row.name, ''), inventory_item_id: textValue(row.inventory_item_id, ''), supplier_id: textValue(row.supplier_id, ''), purchase_price_per_kg: textValue(row.purchase_price_per_kg, ''), grams_left: textValue(row.grams_left, '') }); if (section === 'suppliers') setSupplierForm({ name: textValue(row.name, ''), contact_info: textValue(row.contact_info, '') }); if (section === 'purchase-orders') setPOForm({ supplier_id: textValue(row.supplier_id, ''), oil_inventory_id: textValue(row.oil_inventory_id, ''), quantity_grams: textValue(row.quantity_grams, ''), unit_price_per_kg: textValue(row.unit_price_per_kg, ''), purchase_date: textValue(row.purchase_date, '').slice(0, 10) }); if (section === 'manufacturing') { const firstOil = Array.isArray(row.oils) ? row.oils[0] as Row : {}; const firstProduct = Array.isArray(row.products) ? row.products[0] as Row : {}; setManufacturingForm({ manufacturing_date: textValue(row.manufacturing_date, '').slice(0, 10), notes: textValue(row.notes, ''), oil_inventory_id: textValue(firstOil.oil_inventory_id, ''), quantity_grams: textValue(firstOil.quantity_grams, ''), inventory_item_id: textValue(firstProduct.inventory_item_id, ''), quantity_produced: textValue(firstProduct.quantity_produced, '') }) } setModal(true) }

  const save = async (event: FormEvent) => {
    event.preventDefault(); setIsWorking(true); setError('')
    try {
      let path = ''; const method = editingId ? 'PUT' : 'POST'; let body: unknown
      if (section === 'oils') { path = '/api/inventory/oil'; body = { ...oilForm, id: editingId ? Number(editingId) : undefined, inventory_item_id: oilForm.inventory_item_id ? Number(oilForm.inventory_item_id) : null, supplier_id: oilForm.supplier_id ? Number(oilForm.supplier_id) : null, purchase_price_per_kg: oilForm.purchase_price_per_kg ? Number(oilForm.purchase_price_per_kg) : null, grams_left: oilForm.grams_left ? Number(oilForm.grams_left) : null } }
      else if (section === 'suppliers') { path = '/api/inventory/suppliers'; body = { ...supplierForm, id: editingId ? Number(editingId) : undefined } }
      else if (section === 'purchase-orders') { path = '/api/inventory/po'; body = { id: editingId ? Number(editingId) : undefined, supplier_id: Number(poForm.supplier_id), oil_inventory_id: Number(poForm.oil_inventory_id), quantity_grams: Number(poForm.quantity_grams), unit_price_per_kg: Number(poForm.unit_price_per_kg), total_price: Number(((Number(poForm.quantity_grams) / 1000) * Number(poForm.unit_price_per_kg)).toFixed(2)), purchase_date: `${poForm.purchase_date}T00:00:00Z` } }
      else { path = '/api/inventory/manufacturing'; body = { id: editingId ? Number(editingId) : undefined, notes: manufacturingForm.notes, manufacturing_date: `${manufacturingForm.manufacturing_date}T00:00:00Z`, oils: manufacturingForm.oil_inventory_id ? [{ oil_inventory_id: Number(manufacturingForm.oil_inventory_id), quantity_grams: Number(manufacturingForm.quantity_grams), deduct_inventory: true }] : [], products: manufacturingForm.inventory_item_id ? [{ inventory_item_id: Number(manufacturingForm.inventory_item_id), quantity_produced: Number(manufacturingForm.quantity_produced), add_stock: true }] : [] } }
      await apiRequest(token, onUnauthorized, path, { method, headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); setNotice(`${title} ${editingId ? 'updated' : 'created'}`); setModal(false); await load()
    } catch (caughtError) { setError(caughtError instanceof Error ? caughtError.message : `Unable to save ${title.toLowerCase()}`) } finally { setIsWorking(false) }
  }
  const remove = async (row: Row) => { if (!window.confirm(`Delete this ${title.toLowerCase().replace(/s$/, '')}?`)) return; const id = textValue(row.id, ''); setIsWorking(true); try { await apiRequest(token, onUnauthorized, `${section === 'oils' ? '/api/inventory/oil' : section === 'suppliers' ? '/api/inventory/suppliers' : section === 'purchase-orders' ? '/api/inventory/po' : '/api/inventory/manufacturing'}?id=${id}`, { method: 'DELETE' }); setNotice('Record deleted'); await load() } catch (caughtError) { setError(caughtError instanceof Error ? caughtError.message : 'Unable to delete record') } finally { setIsWorking(false) } }

  return <section className="inventory-operations"><header className="workspace-page-header"><div><p className="eyebrow">Inventory / {title}</p><h2>{title} with the operational detail intact.</h2><p>Manage this inventory workflow with compact forms that stay usable on a small screen.</p></div><div className="support-header-actions"><button className="secondary-button" type="button" onClick={() => void load()} disabled={isLoading}><RefreshCw size={15} className={isLoading ? 'spin' : undefined} aria-hidden="true" /> Refresh</button><button className="primary-button" type="button" onClick={openCreate}><Plus size={15} aria-hidden="true" /> Add record</button></div></header>{error && <div className="dashboard-error" role="alert"><CircleAlert size={18} aria-hidden="true" /><span>{error}</span><button type="button" onClick={() => void load()}>Try again</button></div>}{notice && <div className="inventory-notice" role="status">{notice}</div>}<div className="inventory-summary-grid"><div className="inventory-summary-card"><span className="metric-label">{summary.primaryLabel}</span><strong>{summary.primary.toLocaleString('en-IN')}</strong><small>Visible records</small></div><div className="inventory-summary-card"><span className="metric-label">{summary.secondaryLabel}</span><strong>{section === 'purchase-orders' ? formatMoney(summary.secondary) : Number(summary.secondary).toLocaleString('en-IN')}</strong><small>Across this workspace</small></div></div><section className="orders-card"><div className="orders-card-heading"><div><p className="eyebrow">{title}</p><h3>{rows.length} records</h3></div><span className="inventory-operation-icon">{icon}</span></div><div className="orders-table-wrap"><table className="orders-table"><caption className="sr-only">{title}</caption><thead>{section === 'oils' ? <tr><th>Oil</th><th>Supplier</th><th>Stock</th><th>Purchase price</th><th>Actions</th></tr> : section === 'suppliers' ? <tr><th>Supplier</th><th>Contact</th><th>Oils</th><th>Added</th><th>Actions</th></tr> : section === 'purchase-orders' ? <tr><th>Purchase date</th><th>Supplier</th><th>Oil</th><th>Quantity</th><th>Total</th><th>Actions</th></tr> : <tr><th>Manufactured</th><th>Oils used</th><th>Products made</th><th>Notes</th><th>Actions</th></tr>}</thead><tbody>{isLoading ? <tr><td colSpan={6} className="table-state">Loading {title.toLowerCase()}…</td></tr> : rows.length === 0 ? <tr><td colSpan={6} className="table-state">No {title.toLowerCase()} records found.</td></tr> : rows.map((row, index) => { const id = textValue(row.id, String(index)); const action = <div className="table-action-group"><button className="table-link-button" type="button" onClick={() => openEdit(row)}><Edit3 size={13} aria-hidden="true" /> Edit</button><button className="table-link-button" type="button" onClick={() => void remove(row)} disabled={isWorking}><Trash2 size={13} aria-hidden="true" /> Delete</button></div>; if (section === 'oils') return <tr key={id}><td><strong>{textValue(row.name, 'Unnamed oil')}</strong><small className="table-subtext">{nestedName(row.inventory_item)}</small></td><td>{nestedName(row.supplier)}</td><td className="table-money">{numberValue(row.grams_left).toLocaleString('en-IN')} g</td><td className="table-money">{formatMoney(row.purchase_price_per_kg)}/kg</td><td>{action}</td></tr>; if (section === 'suppliers') return <tr key={id}><td><strong>{textValue(row.name, 'Unnamed supplier')}</strong></td><td>{textValue(row.contact_info)}</td><td>{numberValue(row.oils_count)}</td><td>{formatDate(row.created_at)}</td><td>{action}</td></tr>; if (section === 'purchase-orders') return <tr key={id}><td>{formatDate(row.purchase_date)}</td><td>{nestedName(row.supplier)}</td><td>{nestedName(row.oil_inventory)}</td><td>{numberValue(row.quantity_grams).toLocaleString('en-IN')} g</td><td className="table-money">{formatMoney(row.total_price)}</td><td>{action}</td></tr>; return <tr key={id}><td>{formatDate(row.manufacturing_date)}</td><td>{Array.isArray(row.oils) ? row.oils.map((oil) => `${numberValue((oil as Row).quantity_grams)}g`).join(', ') || '—' : '—'}</td><td>{Array.isArray(row.products) ? row.products.map((product) => `${numberValue((product as Row).quantity_produced)}×`).join(', ') || '—' : '—'}</td><td>{textValue(row.notes)}</td><td>{action}</td></tr> })}</tbody></table></div></section>{modal && <div className="modal-scrim" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) setModal(false) }}><form className="modal-card" role="dialog" aria-modal="true" aria-labelledby="inventory-operation-modal" onSubmit={save}><div className="modal-heading"><div><p className="eyebrow">Inventory / {title}</p><h2 id="inventory-operation-modal">{editingId ? 'Edit' : 'Add'} {title.toLowerCase()}</h2></div><button className="icon-button" type="button" aria-label="Close" onClick={() => setModal(false)}><X size={19} aria-hidden="true" /></button></div>{section === 'oils' ? <><label className="form-field"><span>Oil name</span><input required value={oilForm.name} onChange={(event) => setOilForm({ ...oilForm, name: event.target.value })} /></label><div className="form-grid-two"><label className="form-field"><span>Product link</span><select value={oilForm.inventory_item_id} onChange={(event) => setOilForm({ ...oilForm, inventory_item_id: event.target.value })}><option value="">Unlinked</option>{products.map((product) => <option key={String(product.id)} value={String(product.id)}>{textValue(product.mi_sku)} · {textValue(product.title)}</option>)}</select></label><label className="form-field"><span>Supplier</span><select value={oilForm.supplier_id} onChange={(event) => setOilForm({ ...oilForm, supplier_id: event.target.value })}><option value="">Unassigned</option>{suppliers.map((supplier) => <option key={String(supplier.id)} value={String(supplier.id)}>{textValue(supplier.name)}</option>)}</select></label><label className="form-field"><span>Purchase price / kg</span><input type="number" min="0" step="0.01" value={oilForm.purchase_price_per_kg} onChange={(event) => setOilForm({ ...oilForm, purchase_price_per_kg: event.target.value })} /></label><label className="form-field"><span>Grams left</span><input type="number" min="0" step="0.01" value={oilForm.grams_left} onChange={(event) => setOilForm({ ...oilForm, grams_left: event.target.value })} /></label></div></> : section === 'suppliers' ? <><label className="form-field"><span>Supplier name</span><input required value={supplierForm.name} onChange={(event) => setSupplierForm({ ...supplierForm, name: event.target.value })} /></label><label className="form-field"><span>Contact information</span><textarea rows={3} value={supplierForm.contact_info} onChange={(event) => setSupplierForm({ ...supplierForm, contact_info: event.target.value })} /></label></> : section === 'purchase-orders' ? <><div className="form-grid-two"><label className="form-field"><span>Supplier</span><select required value={poForm.supplier_id} onChange={(event) => setPOForm({ ...poForm, supplier_id: event.target.value })}><option value="">Select supplier</option>{suppliers.map((supplier) => <option key={String(supplier.id)} value={String(supplier.id)}>{textValue(supplier.name)}</option>)}</select></label><label className="form-field"><span>Oil</span><select required value={poForm.oil_inventory_id} onChange={(event) => setPOForm({ ...poForm, oil_inventory_id: event.target.value })}><option value="">Select oil</option>{oils.map((oil) => <option key={String(oil.id)} value={String(oil.id)}>{textValue(oil.name)}</option>)}</select></label><label className="form-field"><span>Quantity in grams</span><input required type="number" min="0" step="0.01" value={poForm.quantity_grams} onChange={(event) => setPOForm({ ...poForm, quantity_grams: event.target.value })} /></label><label className="form-field"><span>Price per kg</span><input required type="number" min="0" step="0.01" value={poForm.unit_price_per_kg} onChange={(event) => setPOForm({ ...poForm, unit_price_per_kg: event.target.value })} /></label></div><label className="form-field"><span>Purchase date</span><input required type="date" value={poForm.purchase_date} onChange={(event) => setPOForm({ ...poForm, purchase_date: event.target.value })} /></label></> : <><div className="form-grid-two"><label className="form-field"><span>Production date</span><input required type="date" value={manufacturingForm.manufacturing_date} onChange={(event) => setManufacturingForm({ ...manufacturingForm, manufacturing_date: event.target.value })} /></label><label className="form-field"><span>Notes</span><input value={manufacturingForm.notes} onChange={(event) => setManufacturingForm({ ...manufacturingForm, notes: event.target.value })} /></label><label className="form-field"><span>Oil used</span><select value={manufacturingForm.oil_inventory_id} onChange={(event) => setManufacturingForm({ ...manufacturingForm, oil_inventory_id: event.target.value })}><option value="">No oil deduction</option>{oils.map((oil) => <option key={String(oil.id)} value={String(oil.id)}>{textValue(oil.name)}</option>)}</select></label><label className="form-field"><span>Oil grams</span><input type="number" min="0" step="0.01" value={manufacturingForm.quantity_grams} onChange={(event) => setManufacturingForm({ ...manufacturingForm, quantity_grams: event.target.value })} /></label><label className="form-field"><span>Product made</span><select value={manufacturingForm.inventory_item_id} onChange={(event) => setManufacturingForm({ ...manufacturingForm, inventory_item_id: event.target.value })}><option value="">No stock addition</option>{products.map((product) => <option key={String(product.id)} value={String(product.id)}>{textValue(product.mi_sku)} · {textValue(product.title)}</option>)}</select></label><label className="form-field"><span>Units produced</span><input type="number" min="0" value={manufacturingForm.quantity_produced} onChange={(event) => setManufacturingForm({ ...manufacturingForm, quantity_produced: event.target.value })} /></label></div></>}<div className="modal-actions"><button className="secondary-button" type="button" onClick={() => setModal(false)}>Cancel</button><button className="primary-button" type="submit" disabled={isWorking}><Plus size={14} aria-hidden="true" /> {isWorking ? 'Saving…' : 'Save record'}</button></div></form></div>}</section>
}
